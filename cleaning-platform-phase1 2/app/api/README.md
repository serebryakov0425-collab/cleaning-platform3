Reserved for Route Handlers that need an HTTP contract outside of React
rendering (e.g. a Telegram bot webhook, a provider notification webhook).
Not used in Phase 1 — see architecture doc, section 5 ("API / Server
Actions архитектура") for why Server Actions are the default and this
directory is the exception, not the rule.
